<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Conductor Panel</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #f8f9fa;
    }
    .card {
      border-radius: 12px;
    }
  </style>
</head>

<body>
  <div class="container mt-5">
    <h2 class="text-center mb-4">🧑‍✈️ Conductor Panel</h2>

    <form action="/conductor" method="POST" class="card p-4 shadow-sm mx-auto" style="max-width: 400px;">
      
      <!-- Secret Key Input -->
      <div class="mb-3">
        <label for="secretKey" class="form-label">Enter Secret Key</label>
        <input 
          type="text" 
          id="secretKey" 
          name="secretKey" 
          class="form-control" 
          placeholder="Enter bus secret key" 
          required
        >
      </div>

      <!-- 🚌 Bus Number Input -->
      <div class="mb-3">
        <label for="busNumber" class="form-label">Bus Number</label>
        <input 
          type="text" 
          id="busNumber" 
          name="busNumber" 
          class="form-control text-uppercase" 
          placeholder="e.g. KA12HG1234" 
          pattern="[A-Z0-9-]+" 
          title="Enter a valid bus number (e.g., KA12HG1234)" 
          required
        >
        <div class="form-text">Enter your bus registration number in uppercase.</div>
      </div>

      <button type="submit" class="btn btn-primary w-100">🔍 Verify</button>
    </form>
  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
