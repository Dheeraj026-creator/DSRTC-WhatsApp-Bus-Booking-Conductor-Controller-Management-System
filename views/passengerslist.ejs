<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Passenger List</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body { background-color: #f8f9fa; }
    .container { margin-top: 40px; }
  </style>
</head>

<body>
  <div class="container">
    <h2 class="text-center mb-4">🚌 Passenger List for Bus <%= busNumber %></h2>

    <table class="table table-striped table-bordered">
      <thead class="table-dark">
        <tr>
          <th>#</th>
          <th>Booking ID</th>
          <th>Passenger Name</th>
          <th>Destination</th>
          <th>Passengers</th>
          <th>Total Fare (₹)</th>
        </tr>
      </thead>
      <tbody>
        <% bookings.forEach((b, index) => { %>
          <tr>
            <td><%= index + 1 %></td>
            <td><%= b._id %></td>
            <td><%= b.user ? b.user.name || 'N/A' : 'N/A' %></td>
            <td><%= b.destination %></td>
            <td><%= b.passengers %></td>
            <td><%= b.totalBill %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="text-center mt-4">
      <a href="/scanner" class="btn btn-primary">🔄 Scan Another Ticket</a>
      <a href="/conductor" class="btn btn-secondary">🏠 Back</a>
    </div>
  </div>
</body>
</html>
